

import argonaut.DecodeJson

import scala.io.{BufferedSource, Source}

object ArgonautJsonTest {
// extends EarthquakeParser with App {
//
//  def testDecodeEvent(implicit decodeJson: DecodeJson[EarthquakeEvent] ) = {
//    val stream: BufferedSource = Source.fromInputStream(getClass.getClassLoader.getResourceAsStream("event.json"))
//    val json = stream.mkString("")
////    val event: Option[EarthquakeEvent] = json.decodeOption[EarthquakeEvent]
//    println(event)
//
//  }

}
